
export struct range {
  int start;
  int finish;
};

export struct range_iterator {
  int elem;
};

export int begin(range* r) {
  return r->start;
}

export int end(range* r) {
  return r->finish;
}

