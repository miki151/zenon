// 37

struct X {
  int operator[](int a) {
    return 2 * a;
  }
};

struct Y {
  X x;
};

X fun() {
  return X();
}

int main() {
  auto x = X();
  auto y = Y(X());
  return 1 + x[1 + x[1 * 5]] + fun()[x[1] + 1] + y.x[1 + y.x[1] + 1];
}
