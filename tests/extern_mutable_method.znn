// no_compile

embed {

#include <vector>
using namespace std;

}

template <T>
extern struct vector {
  vector();
  vector(int cnt, T);
  void push_back(T) mutable;
  T at(int index);
  int size();
//  T operator [](int);
};

int main() {
  auto v = vector<int>();
  v.push_back(3);
  return v.size();
}

