// no_compile

struct X {
  int operator[](int a) {
    return 2 * a;
  }
  string operator[](int a) {
    return "a";
  }
};

struct Y {
  X x;
};

int main() {
  auto x = X();
  auto y = Y(X());
  return 1 + x[1 + x[1 * 5]] + x[x[1] + 1] + y.x[1 + y.x[1] + 1];
}
