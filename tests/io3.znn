// 26

import "std/io.znn";
import "std/math.znn";

int main() {
  const path = "tmp.txt";
  const t = "text";
  const n = 13;
  file_output(path).printf("{t} {n}\n");
  const input = file_input(path);
  const text = *input.scan_string(10);
  const v1 = *input.scan_int();
  const v2 = input.scan_double() ?? 4.6;
  assert(!remove_file(path));
  return text.size() + v1 + as_int(2 * v2).as_int();
}
