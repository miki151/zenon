// 33

enum E {
  E1,
  E2,
  E3,
  E4,
  E5
};

int main() {
  mutable sum = enum_count(E);
  for (i = 0; i < enum_count(E); ++i) {
    const elem = enum_strings(E)[i];
    if (elem == "E2")
      sum += 20;
    if (!(elem == "E1"))
      sum += 2;
  }
  return sum;
}
