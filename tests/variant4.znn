// no_compile

variant V {
  int a;
  bool b;
};

void fun(V* v) {
  switch (*v) {
    case (int a) {
    }
    case (int a) {
    }
    case (bool b) {
    }
  }
}

int main() {
  auto v = V::a(1);
  fun(&v);
  return 0;
}
