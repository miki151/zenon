// 12

template<T>
struct X {
  T operator[](T a) {
    return a;
  }
};

template<T>
struct Y {
  X<T> x;
};

X<int> fun() {
  return X<int>();
}

int main() {
  auto x = X<int>();
  auto y = Y<int>(X<int>());
  return 1 + x[1 + x[1 * 5]] + fun()[x[1] + 1] + y.x[1 + y.x[1] + 1];
}
