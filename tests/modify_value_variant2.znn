// no_compile

variant V {
  int i;
  string s;
};

V getV() {
  return V::s("pok");
}

int main() {
  const v = V::i(5);
  switch (getV()) {
    case (string mutable* s) {
      *s = "pak";
    }
    default {
    }
  }
  return 1;
}
