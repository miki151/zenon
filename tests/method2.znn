// 5

template <T>
concept can_copy {
  T(T*);
};

template <T>
struct X {
  T v;
};

template<T> requires can_copy<T>
void set(mutable X<T>* x, T t) {
  (*x).v = t;
}

template <T>
T* get(X<T>* x) {
  return &(*x).v;
}

int main() {
  mutable x = X<int>(10);
  x.set(5);
  return *x.get();
}
