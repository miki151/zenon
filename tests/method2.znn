// 5

template <T>
concept can_copy {
  T(T*);
};

template <T>
struct X {
  T* get() {
    return &v;
  }
  T v;
};

template<T> requires can_copy<T>
void set(mutable X<T>* x, T t) {
  (*x).v = t;
}

int main() {
  mutable x = X<int>(10);
  x.set(5);
  return *x.get();
}
