// 6

union V {
  int a;
  bool b;
};

int fun(V* v) {
  switch (v) {
    case (int* a) {
      return *a;
    }
    case (bool* b) {
      if (*b)
        return 2;
      else
        return 1;
    }
  }
}


int main() {
  return V::a(3).fun() + V::b(false).fun() + V::b(true).fun();
}
