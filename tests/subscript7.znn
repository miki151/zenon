// 7

template<T>
concept can_copy {
    T(T&);
};

struct X {
};

template <T> requires can_copy<T>
T operator[](X x, T a) {
  return a;
}

int main() {
  auto x = X();
  int i = 7;
  return x[i];
}
