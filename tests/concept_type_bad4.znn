// no_compile

template <T, U>
concept C1 {
  T call(T*, int);
  U call2(U*, int);
};

struct X {
};

X call(X*, int) {
  return X();
}

int main() {
  const x = X();
  const c1 = cast<C1*>(&x);
  return 0;
}
