
template<T>
concept can_copy {
  T(T*);
};

template<T>
struct X {
  T* get() {
    return &v;
  }

  T v;
};

template <T>
concept can_copy {
  T(T*);
};

template<T> requires can_copy<T>
void set(mutable X<T>* x, T t) {
  (*x).v = t;
}


