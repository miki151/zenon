// 7
import "struct1.znn";
import "struct2.znn";

int get() {
  V2<int, bool> v2 = V2<int, bool>::t(5);
  const x = v2.get<int>(1, true, 2);
  switch (v2) {
    case (int t) {
      return t + x;
    }
    case (bool u) {
      return 1;
    }
  }
}

int main() {
  V1 v = V1::x(get());
  switch (v) {
    case (int x) {
      return x;
    }
    case (bool b) {
      return 0;
    }
  }
}
