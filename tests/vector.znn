// 55

import "std/vector.znn";

int f() {
  const v = vector<int>();
  return v.size();
}

int main() {
  mutable v = vector<int>();
  for (mutable i = 0; i < 6; i = i + 1)
    v.push_back(i * i);
  mutable sum = 0;
  for (mutable i = 0; i < v.size(); i = i + 1)
    sum = sum + *v.at(i);
  return sum + f();
}

