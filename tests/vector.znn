// 15

embed {

#include <vector>
using namespace std;

}

template <T>
extern struct vector {
  void push_back(T elem);
  T at(int index);
  int size();
};

int main() {
  auto v = vector<int>();
  for (int i = 0; i < 6; i = i + 1)
    v.push_back(i);
  int sum = 0;
  for (int i = 0; i < v.size(); i = i + 1)
    sum = sum + i;
  return sum;
}

