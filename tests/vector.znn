// 55

embed {

#include <vector>
using namespace std;

}

template <T>
extern struct vector {
  vector();
  vector(int cnt, T);
  void push_back(T);
  T at(int index);
  int size();
  T operator [](int);
};


int main() {
  vector<int> v;
  for (mutable i = 0; i < 6; i = i + 1)
    v.push_back(i * i);
  mutable sum = 0;
  for (mutable i = 0; i < v.size(); i = i + 1)
    sum = sum + v[i];
  return sum;
}

