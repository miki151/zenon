// 5

embed {

#include <stdio.h>
#include <vector>
using namespace std;

}

// dupa
void print(int x) {
  embed {
    printf("%d\n", x);  
  }
}

template <T>
extern struct vector;

template<T>
void push_back(vector<T>& v, T elem) {
  embed {
    int embed;
    v.push_back(std::move(elem));
  }
}

template<T>
vector<T> make_vec() {
  embed {
    return vector<T>();
  }
}

template<T>
T get(vector<T>& v, int index) {
  embed {
    return v.at(index);
  }
}

int main() {
  vector<int> v = make_vec<int>();
  push_back(v, 5);
  return get(v, 0);
}

