// 10

struct X {
  int a;
};

X implicit_copy(X* x) {
  return X(10);
}

union U {
  X x;
};

U implicit_copy(U*) = default;

int main() {
  const u = U::x(X(1));
  switch (u) {
    case (X x) {
      return x.a;
    }
  }
}
