// no_compile

concept <T> C1 {
  int get(T*);
  void set(T mutable*, int);
  void destruct(T*);
};

struct X {
  int a;
};

int get(X* x) {
  return x->a;
}

int main() {
  const x = X(5);
  mutable C1 c = move(x);
  return 0;
}
