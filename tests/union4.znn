// no_compile

union V {
  int a;
  bool b;
};

void fun(V* v) {
  switch (*v) {
    case (a) {
    }
    case (a) {
    }
    case (b) {
    }
  }
}

int main() {
  const v = V::a(1);
  fun(&v);
  return 0;
}
