// 5

struct S {
  int a;
};

void f(mutable S* s) {
  (*s).a = 5;
}

variant V {
  int i;
  string s;
};

int get(mutable V* self, int a) {
  mutable s1 = S(1);
  s1.f();
  switch (*self) {
    case (mutable int* i) {
      *i = 4;
      return *i + a;
    }
    case (string s) {
      return 1 + a;
    }
  }
}

int main() {
  int a = 1;
  mutable v = V::i(5);
  return v.get(1);
}
