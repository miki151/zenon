// 5

struct S {
  int a;
};

void f(S* s) {
  (*s).a = 5;
}

variant V {
  int i;
  string s;
};

int get(V* self, int a) {
  mutable s1 = S(1);
  s1.f();
  switch (*self) {
    case (int* i) {
      *i = 4;
      return *i + a;
    }
    case (string s) {
      return 1 + a;
    }
  }
}

int main() {
  int a = 1;
  mutable v = V::i(5);
  return v.get(1);
}
