// 0

import "std/panic.znn";

int main() {
  int? a = 2;
  int? b = 2;
  int? c = 3;
  int d = 2;
  int? e = null;
  assert(a == 2);
  assert(2 == a);
  assert(a == b);
  assert(a == d);
  assert(d == a);
  assert(a != c);
  assert(a != e);
  assert(e != a);
  assert(e == e);
  return 0;
}
