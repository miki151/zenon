// no_compile

template<A1, A2, A3, A4>
union V {
  A1 a1;
  A2 a2;
  A3 a3;
  A4 a4;
};

int main() {
  const v = V<bool, int, string, char>::a1(true);
  switch (move(v)) {
    case (A1 a1) {
      return 1;
    }
    default {
      return 0;
    }
  }
}
