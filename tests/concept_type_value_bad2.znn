// no_compile

template <T>
concept C1 {
  int get(T*);
  void set(T mutable*, int);
  void destruct(T*);
};

struct X {
  int a;
};

int get(X* x) {
  return x->a;
}

void set(X mutable* x, int a) {
  x->a = a;
}

int main() {
  const x = X(5);
  mutable c = cast<C1>(&x);
  return 0;
}
