

public struct X {
  int v;
};

public template<T> requires can_copy<T>
int get(X* x, T u) {
  return x->v;
}

public template <T>
struct Y {
  T v;
};

public template<T, U> requires can_copy<T>
T get(Y<T>* x, U u) {
  return x->v.copy();
}


