
template<T>
concept can_copy {
  T(T*);
};

struct X {
  template<T> requires can_copy<T>
  int get(T u) {
    return v;
  }

  int v;
};


template <T> requires can_copy<T>
struct Y {
  template<U>
  T get(U u) {
    return v;
  }

  T v;
};


