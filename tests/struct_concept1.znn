// 10

template <T>
concept CanAdd {
  T operator + (T t1, T t2);
};

template <A> requires CanAdd<A>
struct S1 {
  A a;
  A get() {
    return a + a;
  }
};

int main() {
  auto s = S1(5);
  auto s2 = S1("pok");
  if (s2.get() == "pokpok")
    return s.get();
  else
    return 0;
}
